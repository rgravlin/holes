version: "2"

linters:
  enable:
    - errorlint
    - gocritic
    - godot
    - gosec
    - misspell
    - nolintlint
    - revive
  settings:
    govet:
      enable:
        - shadow
    nolintlint:
      require-explanation: true
      require-specific: true

formatters:
  enable:
    - gofmt
    - goimports
  settings:
    goimports:
      local-prefixes:
        - github.com/rgravlin/holes
